---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tmodloader-pvc
  namespace: terraria
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
